{
  "name": "verstka",
  "version": "0.1.0",
  "description": "reactless html layout for components reference",
  "dependencies": {
    "bootstrap": "^5.2.3",
    "bootstrap-icons": "^1.10.3"
  },
  "scripts": {
    "dev-server": "live-server",
    "watch-sass": "sass --watch reagirovanie --no-source-map",
    "start": "npm run watch-sass & npm run dev-server"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.59.3"
  }
}
